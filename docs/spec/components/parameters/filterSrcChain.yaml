name: filter[src_chain]
in: query
required: false
description: "Filter by `chain_id` of the order/match source blockchain, related to `data.relationships.src_chain.data.id` field"
schema:
  type: integer
  example: 91
