name: include
in: query
required: false
description: "Include relationships of orders and match orders"
schema:
  type: string
  enum:
    - src_chain
    - destination_chain
    - origin_chain
    - origin_order
    - token_to_sell
    - token_to_buy
